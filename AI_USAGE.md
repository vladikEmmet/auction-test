# Использование AI

Задание разрешает и предполагает работу с AI, поэтому фиксирую честно: код писался в
паре с AI-ассистентом (Claude Code), архитектурные решения и приёмка — за мной.

## Что делалось с AI

- Разбор OpenAPI-схемы: выгрузка всех 47 схем и 8 enum'ов, сверка списков полей.
- Рутинный код по заданному мной каркасу: Zod-схемы DTO, компоненты UI-кита на Radix,
  разметка карточек и блоков детальной страницы, генератор мок-данных, тексты тестов.
- Механические правки по моему разбору: разбиение крупных файлов, вынос повторяющейся
  разметки, замена приведений типов на безопасные конструкции.
- Черновики README и этого файла.

## Какие решения принимал сам

- **Стек внутри обязательного набора:** shadcn-подход (Radix + Tailwind, компоненты в
  репозитории) вместо готовой библиотеки; Zustand вместо MobX; фильтры в URL search
  params, без localStorage-персиста; режим ставки — отдельный роут, отрисованный
  модалкой над деталкой.
- **Схема как источник правды, а не разовый образец.** Вместо генерации типов один раз
  сделал contract-тест, который читает `openapi.auctions.v0.json` при каждом прогоне и
  сверяет поля, enum'ы и nullable. Это ловит расхождение при обновлении схемы.
- **Правило nullable:** поле считается nullable, если стоит `nullable: true` **или** в
  примере указан `null`. В схеме есть поля только со вторым признаком; строгий разбор
  ронял бы валидные ответы.
- **Единые правила ставки** в `shared/lib/bet-rules.ts` для формы и для MSW. Изначально
  напрашивалось продублировать проверки на «сервере» — отказался, иначе 422 и клиентская
  валидация неизбежно разъезжаются.
- **MSW-стор с проекцией, а не с двумя копиями DTO.** Источник правды — детальный DTO,
  элемент списка вычисляется из него. Так список и деталка не могут разойтись после ставки.
- **Разбор расхождений в самой схеме** (узкий `status_mobile` в списке, дубль
  `hide_bets_history`, отсутствие шага ставки в списочном DTO, противоречивые примеры НДС) —
  решения по каждому и их обоснование вынесены в README.

## Какие предложения AI отклонил

- **Загрузка данных в `loader` роутера** (`ensureQueryData`). Красиво, но блокирует
  переход до ответа — задание требует skeleton-состояний. Оставил `useQuery` + явный
  prefetch по hover/focus.
- **Синхронизация черновика фильтров через `useEffect` + `setState`.** Классический
  антипаттерн (и его ловит `react-hooks/set-state-in-effect`). Заменил на пересоздание
  состояния через `key` при изменении URL.
- **Показывать шаг ставки на карточке списка**, доставая его из кэша детального запроса.
  Отказался: в списочном контракте шага нет, подставлять «правдоподобное» значение —
  вранье в UI. Поле на карточке есть (его требует ТЗ), но показывает «— в деталях»
  с пояснением; рядом выведена стартовая цена, которая в контракте присутствует.
- **`react-day-picker` для дат погрузки.** Для двух полей «от/до» нативный `input[type=date]`
  надёжнее и лучше на мобильных; лишнюю зависимость убрал.
- **Мемоизация ради мемоизации** (`useMemo` вокруг вычисления ограничений ставки).
  Функция чистая и дешёвая, а список зависимостей расходился с реальным — убрал.
- **Свести `status_mobile` списка и деталки к одному enum'у.** Это «починка» контракта
  на клиенте; воспроизвожу схему как есть.

## Что проверял особенно внимательно

- **Соответствие контракту**: набор полей, enum'ы и nullable по всем 42 DTO — вынес в
  автотест, а не в ручную сверку.
- **Арифметику ставок**: кратность шага и границы считаются в копейках; отдельный тест
  проверяет, что предложенная по умолчанию цена всегда проходит валидацию.
- **Мутацию MSW**: пересчёт мест (рейтинг по лучшей ставке организации, а не по каждой
  ставке), обновление `available` и `price_per_km`, смену торгового статуса, и то, что
  изменения видны в списке.
- **Разбор search params**: намеренно кормил схему мусором. Здесь же нашёлся реальный
  баг — парсер роутера отдаёт `?cargo_num=99999999999` как число, и фильтр молча
  терялся; поймано интеграционным тестом, исправлено приведением к строке.
- **Edge cases скрытия данных**: `hide_bets_history` (запрос не отправляется вовсе),
  `hide_points_address_and_contacts`, `no_view_cargo_price`, `hide_places`,
  `can_set_bet = false`.
- **Стабильность самих тестов.** Два интеграционных сценария однажды упали без связи с
  правкой. Разбирался до причины, а не перезапускал: после перевода страниц на ленивую
  загрузку первому рендеру нужно ещё и разрешить динамический импорт чанка, и дефолтной
  секунды у `findBy`/`waitFor` под нагрузкой не хватало. Поднял таймауты ожидания и
  проверил тремя прогонами при параллельных `build` и `tsc`.

## Что нашёл и починил при самопроверке

Отдельно прошёлся по коду после того, как всё заработало, — нашёл четыре дефекта:

1. На детальной странице ветка «Смотреть ставки» попадала в `else` и рисовалась как
   **disabled**-кнопка «Ставка недоступна»: участник закрытых торгов упирался в тупик,
   хотя на карточке списка та же кнопка работала.
2. `AuctionDetailVm` не отдавал `is_bidder`, из-за чего участие подменялось проверкой
   `your.bet`. После отмены ставки пользователь терял доступ к истории торгов.
3. Валюта была захардкожена рублём, хотя в DTO есть `payment.currency_code`.
   Добавил `currencyFromCode` и проброс кода через ViewModel во все места с деньгами.
4. В глобальных стилях был объявлен `@custom-variant dark`, но ни одного тёмного токена
   и ни одного `dark:`-класса — мёртвый код в самом заметном файле. Убрал; позже тёмная
   тема была сделана по-настоящему, вместе с токенами и переключателем.

Позже нашёлся пятый: сид создавал ставки с `place: null` и нигде их не ранжировал —
пересчёт мест жил только в мутации. В итоге в 49 аукционах из 49 колонка «Место» была
пустой до первой ставки, и это выглядело как `hide_places`. Пересчёт вынесен в общий
модуль `msw/ranking.ts` и вызывается и сидом, и мутацией; регрессия закрыта тестом,
который требует, чтобы пустые места остались ровно у одного аукциона — того, где
организатор действительно скрыл рейтинг.

На каждый пункт есть тест; первый ловится интеграционным сценарием, который до
исправления падал.

## Что доделал после ревью

Помимо четырёх дефектов выше — пять улучшений UI/UX, каждое с чистым ядром и тестами:

1. **Обратный отсчёт до конца торгов** на карточке и деталке. Расчёт вынесен в чистую
   `getTimeLeft(target, now)`, хук только тикает раз в секунду и останавливается по
   истечении. Истёкшее время гасит кнопку ставки и подменяет форму объяснением.
2. **Чипсы применённых фильтров** над списком: `describeActiveFilters` — чистая функция,
   отдающая подписи и патчи; снятие одного значения из мультивыбора не трогает остальные.
3. **Кнопки «−шаг» / «+шаг» и превью суммы без НДС** в форме ставки. Коэффициент НДС
   выводится из пары цен DTO (`current` / `current_no_vat`), а не зашит константой.
4. **Стартовая цена на карточке** и убранное из ViewModel мёртвое поле `step: null`.
   Позже, при сплошной сверке с ТЗ, поле «шаг ставки» вернулось на карточку — задание
   требует его явным пунктом, — но с честным «— в деталях» вместо выдуманного числа.
5. **Сортировка истории ставок** по месту, цене и времени с `aria-sort`; ставки без места
   всегда внизу независимо от направления сортировки.
6. **Панель фильтров, которая не требует прокрутки к началу.** Сверху форма развёрнута
   целиком, при прокрутке строка сортировки и чипсов прилипает под шапкой и получает
   кнопку вызова тех же фильтров в выезжающей панели. Момент ухода панели из вида ловит
   `IntersectionObserver`, поля описаны один раз в `FiltersForm` и переиспользуются обеими
   обёртками.
7. **Тёмная тема.** Палитра описана токенами, переключает её класс `dark` на `<html>`,
   поэтому `dark:`-классов в компонентах нет. Скрипт в `index.html` применяет тему до
   первого кадра, `color-scheme` переключается вместе с палитрой (иначе нативный календарь
   в фильтрах остался бы светлым). Подложку модалок пришлось перевести на отдельный токен:
   `bg-foreground/40` в тёмной теме давала светлую вуаль.
8. **Переключатель темы переделан** из одной цикличной кнопки в группу из трёх: по иконке
   монитора нельзя догадаться, что это выбор темы, а первый клик в цикле часто не давал
   видимого эффекта (системная тема и так светлая).

## Кодстайл-правки после ревью

- `store.ts` на 466 строк разложен на `db` / `projections` / `queries` / `mutations` / `vat`;
  сам `store.ts` остался публичным API этой пятёрки, импорты в коде и тестах не менялись.
- Бутстрап MSW из двух интеграционных тестов вынесен в `src/testing/integration-setup.ts`,
  туда же переехал рендер приложения в памяти — тестовые помощники больше не живут в слое `app`.
- `statusBadgeVariant` переехал из `widgets/auction-card/lib` в `entities/auction/lib`:
  маппинг enum → вариант бейджа относится к сущности, а деталка тянула ради него виджет.
- Убран лишний реэкспорт словаря городов из `msw/dictionaries.ts`.
- Страницы и моки разделены на чанки: основной бандл ~290 кБ вместо 1,1 МБ.

Второй заход — после сплошного обзора архитектуры:

- Генератор мок-данных (595 строк) разложен на `seed/{lib,edge-cases,builders,index}`;
  сборка записи ужата с 266 строк до 182 выносом построения маршрута, груза, торгов и оплаты.
- `AuctionDetailVm` читается в 25 строк вместо 110 (группы вынесены в отдельные типы),
  маппер стал композицией из семи функций вместо одного 120-строчного литерала.
- В таблице ставок строка описывалась дважды — для таблицы и для мобильных карточек;
  вынесены `BetRow` и `BetCard`.
- Повторяющаяся обёртка поля фильтра вынесена в `FilterField`, который сам выдаёт `id`
  и связывает его с подписью; выбор города — в `CityFilter`.
- Убраны все приведения типов и non-null: было 4 `as` и 4 `!`, стало 0 и 0. Значения
  Radix Select разбираются теми же Zod-схемами, что и URL; сужение статуса — через
  type predicate; индексация справочников — через `pick()` с внятной ошибкой.

## Комментарии и коммиты

Комментарии из кода убраны, коммиты приведены к однострочным сообщениям на английском.
В ТЗ на этот счёт требований нет — это моё решение по стилю репозитория. Плата: «почему»
теперь живёт только в README и в этом файле, поэтому оба документа приходится держать
в актуальном состоянии. Форматирование зафиксировано `.prettierrc.json`, чтобы стиль
не зависел от настроек редактора.

## Решения, которые пришлось защищать отдельно

- **Ставка не заменяется, а добавляется в журнал.** Проверка «почему мои старые ставки
  остались» упирается в контракт: `BetItem` с `created_at` и `cancel_reason` плюс параметр
  `all` у `GET /bets` описывают историю торгов, а ТЗ отдельно требует «количество
  участников» — этот пункт был бы бессмыслен, будь у каждого перевозчика одна строка.
  Поведение оставил, но убрал вводящий в заблуждение статус: прошлые ставки теперь
  «Неактуальна», а не «Активна».
- **Сброс мок-данных при перезагрузке.** ТЗ требует изменения состояния после мутаций и
  просит описать оставшиеся ограничения — персистентность не оговорена. Вместо лишнего
  кода добавил напоминание в success-тост.

## Оставшиеся риски

- **Мок — не реальный upstream.** Все проверки контракта идут против схемы, а не против
  живого сервиса; поведение реального API при 401/503 и при отклонениях от схемы
  проверить негде. Клиент к этому готов (мягкий разбор + warning в dev), но это допущение.
- **Ставка НДС 20 % зашита в мок.** В схеме `current_vat_rate` — строка на уровне
  организации; при других ставках цифры «без НДС» в моке будут неверны. Валюта, в отличие
  от ставки НДС, берётся из DTO.
- **Смысл `your.last_bet` в списке** выведен из примера, а не из документации: если
  upstream шлёт там цену без НДС, карточка покажет не то число.
- **Время торгов проверяется только на клиенте.** Мок не сверяет `stop_time` при ставке,
  поэтому UI и «сервер» здесь расходятся: реальный upstream ставку бы отклонил.
- **Направление торгов для `Request`** трактуется как «любая цена > 0 в границах
  min/max» — в схеме правил на этот счёт нет.
- **Интеграционные тесты идут через jsdom** с заглушками ResizeObserver и pointer-capture;
  реальное поведение Radix-поповеров и мобильных раскладок ими не проверяется.
- **Нет e2e и визуальных проверок**: адаптив и поведение поповеров проверяются вручную
  по чек-листу из README, автоматики на это нет.
- **Обоснования решений живут только в документации.** Комментариев в коде нет, поэтому
  README и этот файл — единственный источник «почему». Если они разойдутся с кодом,
  восстановить логику решений будет неоткуда.

## Что улучшил бы за ещё один день

1. Playwright: 3–4 e2e-сценария в реальном браузере (фильтры, ставка, мобильная ширина) —
   закрыть то, что не ловит jsdom.
2. Оптимистичное обновление ставки с откатом по ошибке.
3. Спрятать MSW за флаг сборки, чтобы прод-версия с реальным API вообще не тащила моки
   (сейчас они в отдельном чанке, но грузятся всегда).
4. Довести фильтры до полного `AuctionListRequest` (вес, объём, тип кузова, заказчик,
   радиусы городов) и сохранять последний набор фильтров в localStorage поверх URL.
5. Кодогенерацию типов из OpenAPI в CI рядом с contract-тестом: сейчас Zod-схемы
   написаны руками, генерация убрала бы класс ошибок «забыли поле».
6. Доступность: пройти axe по всем экранам, проверить фокус-ловушки в модалке и
   озвучивание ошибок формы скринридером.
7. Вернуть точечные комментарии там, где решение продиктовано противоречием в схеме
   (`hide_places`, двойной `hide_bets_history`, вывод ставки НДС): сейчас за объяснением
   нужно идти в README.
